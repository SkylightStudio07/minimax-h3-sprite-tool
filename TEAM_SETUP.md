# 팀용 Sprite Lab

## 기본 생성 모드 변경

일반 20스텝 (Turbo LoRA 없음), 네거티브 꺼짐이 기본입니다. 생성 화면에서 Turbo 4스텝을 빠른 테스트용으로 선택할 수 있습니다. 일반 모드는 훨씬 오래 걸리지만 고정 시드 비교에서 배경 유지가 개선되었습니다. 생성 기록에는 모드와 실제 스텝을 함께 저장합니다.

## 2026-09-08 갤러리·생성 화면 업데이트

- 프레임 수는 2~32 정수 직접 입력을 지원합니다. 영상 생성 길이/FPS는 그대로이며 시트에서 고르는 프레임 수만 바뀝니다.
- 승인된 팀원은 대기·진행 중 작업의 입력 이미지 썸네일을 볼 수 있습니다. 완료·취소 후 원본 payload는 기존 정책대로 제거됩니다.
- 로그인 전 메인 화면에 공개 갤러리가 표시됩니다. 기존 완료 결과를 포함하여 완성 영상과 시트가 공개되며, H3 원본 영상·입력 이미지·작업자 이름·계정 정보는 공개 API에 포함되지 않습니다.
- 영상 전체화면 버튼과 새 탭 링크를 추가하고 생성 화면 iframe에 전체화면 권한을 부여했습니다.
- 현재 외부 접속 주소: https://bcsd-nai.mywire.org:18444/ (기존 `/sprite/` 계획 대신 별도 포트 사용).
- 외부 서비스 실행에는 `start_team_tool_https.bat`을 사용합니다.

## 로컬 실행

1. 기존 `start_idle_tool.bat` 서버가 실행 중이면 종료합니다. 두 서버는 같은 7866 포트를 사용합니다.
2. `.venv\Scripts\python.exe -m pip install -r requirements-tool.txt`
3. `start_team_tool.bat` 실행 후 http://127.0.0.1:7866/ 접속.
4. 최초 등록 화면에서 본인이 정한 아이디·표시 이름·비밀번호(10자 이상)와 `data/admin-setup-code.txt`의 코드를 입력합니다.
5. 등록한 관리자 계정으로 로그인합니다. 설정 코드는 사용 즉시 삭제됩니다. 코드나 비밀번호를 Git에 올리지 마세요.
6. 팀원은 **가입 요청**을 하고, 관리자는 **계정 승인**에서 승인합니다.

## 공유와 대기열

- 승인된 모든 팀원이 작업자 이름·대기 순서·생성 단계·원본 미리보기를 볼 수 있습니다. 완성 영상과 시트는 비로그인 방문자에게도 공개됩니다.
- GPU 작업은 FIFO로 한 번에 하나씩 처리합니다. 개인 미완료 작업 3개, 전체 20개 제한입니다.
- 본인 또는 관리자가 아직 시작하지 않은 작업을 취소할 수 있습니다. 진행 중인 GPU 작업은 강제 중단하지 않습니다.
- 계정 이용 제한은 다음 요청부터 적용되고 해당 계정의 대기 작업은 취소됩니다.
- 페이지를 닫아도 작업은 유지됩니다. 서버 재시작 시 대기 작업은 남으며, 진행 중이던 작업은 실패로 표시되므로 다시 요청해야 합니다.
- 엔진이 꺼져 있으면 등록된 대기열은 보관됩니다. ComfyUI가 준비되면 순서대로 처리합니다.
- 결과 갤러리에서 시트를 가로 스크롤하거나 원본으로 열고, 시트/정규화 영상/H3 원본 영상을 내려받을 수 있습니다.
- 갤러리는 **팀 서버에서 생성한 작업**을 표시합니다. 기존 단독 서버 결과는 디스크에 그대로 남으며 자동으로 팀에 공유하지 않습니다.
- 계정·대기열·세션 키는 `data/`, 미디어는 `outputs/results/`에 보관됩니다. Git에는 포함되지 않습니다. 백업은 서버를 종료한 뒤 두 폴더를 함께 복사하세요.

## 나중에 NAS 연결

현재는 `127.0.0.1:7866`에만 바인딩되어 외부 공개되지 않습니다. NAS가 복구되면 Tailscale 연결과 역방향 프록시를 별도로 설정해야 합니다.

예정된 외부 주소는 `https://bcsd-nai.mywire.org/sprite/`입니다. 프록시는 `/sprite/` 접두사를 제거하고 앱으로 전달해야 합니다. 예: `/sprite/api/me` → 앱의 `/api/me`. `/sprite` 요청은 `/sprite/`로 리다이렉트하세요. UI의 API·영상·이미지 주소는 이 접두사를 유지합니다.

HTTPS 배포 시 `SPRITE_HTTPS=1`로 실행하여 Secure 쿠키를 사용하세요. 이 옵션 사용 중에는 일반 HTTP로 로그인할 수 없습니다. NAS에서 PC의 루프백 포트로 직접 접근할 수 없으므로 Tailscale의 사설 프록시 등 별도 연결이 필요합니다. ComfyUI 8189 포트나 예전 비인증 서버를 인터넷에 직접 노출하지 마세요. 쿠키, POST 요청, 동영상 Range 헤더를 프록시가 보존해야 합니다.

로그인/가입은 클라이언트 IP별 10분에 30회로 제한합니다. 프록시 환경에서는 여러 팀원이 하나의 IP로 집계될 수 있습니다. 현재는 전달된 IP 헤더를 신뢰하지 않습니다.

## 검증

`.venv\Scripts\python.exe -m unittest test_team_server -v`

임시 DB와 모의 생성기로 승인/차단, CSRF, 입력 검증, 취소 권한, 한도, FIFO, 재시작, 결과 접근 및 Range 응답을 검증합니다. 모델 생성 품질 테스트는 아닙니다.
